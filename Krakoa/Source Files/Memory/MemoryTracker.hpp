#pragma once

namespace krakoa
{
	//void* operator new(size_t bytes, void* ptr);
	//void* operator new[](size_t bytes, void* ptr);
	//void operator delete(void* ptr, size_t bytes);
	//void operator delete[](void* ptr, size_t bytes);
	
}
